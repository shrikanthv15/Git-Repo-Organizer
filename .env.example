# ─────────────────────────────────────────────────
# Git Repo Organizer — Environment Variables
# ─────────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ─────────────────────────────────────────────────

# === Database (Docker Compose) ===
POSTGRES_USER=gardener
POSTGRES_PASSWORD=gardener_secret
POSTGRES_DB=gardener
DATABASE_URL=postgresql://gardener:gardener_secret@postgres:5432/gardener

# === Temporal (Docker Compose) ===
TEMPORAL_ADDRESS=temporal:7233

# === GitHub OAuth ===
# Create at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# === AI / LLM ===
# Your LiteLLM or OpenAI-compatible API
LITELLM_API_KEY=your_litellm_api_key
LITELLM_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# === Frontend ===
# These are used by Next.js at build time
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_client_id
